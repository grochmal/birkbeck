digraph {
  /* the subgraphs need to have distinct IDs
   * to be visually separated in the graph */
  subgraph cluster_0 {
    label="Subgraph A";
    a -> b;
    b -> c;
    c -> d;
  }
  subgraph cluster_1 {
    label="Subgraph B";
    a -> f;
    f -> c;
  }
}

