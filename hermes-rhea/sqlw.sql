select from where and or group having by create drop table view insert set
SELECT FROM WHERE AND OR GROUP HAVING BY CREATE DROP TABLE VIEW INSERT SET
update trigger procedure before after for each when on in referencing after
UPDATE TRIGGER PROCEDURE BEFORE AFTER FOR EACH WHEN ON IN REFERENCING AFTER
replace begin end into values raise declare then row of enable disable alter
REPLACE BEGIN END INTO VALUES RAISE DECLARE THEN ROW OF ENABLE DISABLE ALTER
if sdo_rdf_triple_s sem commit null  count sum avg nvl max min exists not
IF SDO_RDF_TRIPLE_S SEM COMMIT NULL  COUNT SUM AVG NVL MAX MIN EXISTS NOT
off echo feedback heading pagesize pause spool describe user all dict grant
OFF ECHO FEEDBACK HEADING PAGESIZE PAUSE SPOOL DESCRIBE USER ALL DICT GRANT

